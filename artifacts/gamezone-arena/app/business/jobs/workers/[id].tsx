import React from"react";import{Alert,Linking,ScrollView,Text}from"react-native";import{useLocalSearchParams}from"expo-router";import{useSupabaseAuth}from"@/hooks/useBusiness";import{useJobMutation,useMyJobs,useSeekerContact,useWorker}from"@/hooks/useJobs";import{Button,styles as s}from"@/components/JobsUi";import{copyLink,shareLink}from"@/lib/share";
export default function Worker(){useSupabaseAuth();const{id}=useLocalSearchParams<{id:string}>();const{data:w,error}=useWorker(id);const{data:contact}=useSeekerContact(id);const jobs=useMyJobs();const m=useJobMutation();if(!w)return <Text style={s.empty}>{error?(error as Error).message:"Worker unavailable"}</Text>;const open=(u:string)=>void Linking.openURL(u).catch(()=>Alert.alert("Unavailable","No compatible app is available."));const invite=()=>{const active=(jobs.data??[]).filter(j=>j.status==="active");if(!active.length)return Alert.alert("No active jobs","Publish an active job before inviting a worker.");Alert.alert("Invite to apply","Choose one of your active jobs.",[...active.map(j=>({text:j.title,onPress:()=>m.mutate({type:"invite",id:w.id,value:{job_id:j.id}},{onSuccess:()=>Alert.alert("Invite sent","The worker can now respond to your invite."),onError:(e:Error)=>Alert.alert("Could not invite",e.message)})})),{text:"Cancel",style:"cancel"}])};const link=`gamezone-arena://business/jobs/workers/${w.id}`;return <ScrollView style={s.root} contentContainerStyle={s.content}><Text style={s.title}>{w.display_name}</Text><Text style={s.company}>{w.job_role}</Text><Text style={s.subtitle}>{w.bio}</Text>{[["Skills",w.skills.join(", ")],["Experience",w.experience_months==null?null:`${w.experience_months} months`],["Education",w.education],["Preferred work",w.preferred_work_types.join(", ").replaceAll("_"," ")],["Location",[w.area,w.city].filter(Boolean).join(", ")],["Available from",w.available_from]].filter((x):x is [string,string]=>!!x[1]).map(x=><Text key={x[0]} style={s.card}><Text style={s.name}>{x[0]}</Text>{"\n"}<Text style={s.meta}>{x[1]}</Text></Text>)}<Text style={s.subtitle}>Contact actions are available only when server-authorized consent permits them.</Text><Button label="Invite to apply" kind onPress={invite}/>{contact?.phone&&<Button label="Call" onPress={()=>open(`tel:${contact.phone!.replace(/[ ()-]/g,"")}`)}/>} {contact?.whatsapp&&<Button label="WhatsApp" onPress={()=>open(`https://wa.me/${contact.whatsapp!.replace(/\D/g,"")}`)}/>}<Button label="Share profile" onPress={()=>void shareLink(w.display_name,link)}/><Button label="Copy link" onPress={()=>void copyLink(link).then(()=>Alert.alert("Copied","Worker profile link copied.") )}/><Button label="Report" onPress={()=>m.mutate({type:"worker-report",id:w.id,value:"other"},{onSuccess:()=>Alert.alert("Reported","Thank you.")})}/><Button label="Block" onPress={()=>m.mutate({type:"block",id:w.user_id,value:true})}/></ScrollView>}