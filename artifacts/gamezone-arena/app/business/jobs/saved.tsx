import React from"react";import{FlatList,Text,View}from"react-native";import{useRouter}from"expo-router";import{useSupabaseAuth}from"@/hooks/useBusiness";import{useJobMutation,useSavedJobs}from"@/hooks/useJobs";import{Button,Card,styles as s}from"@/components/JobsUi";
export default function Saved(){useSupabaseAuth();const r=useRouter();const{data,error}=useSavedJobs();const m=useJobMutation();return <View style={[s.root,{padding:20}]}><Text style={s.title}>Saved Jobs</Text><FlatList data={data??[]} keyExtractor={x=>x.id} contentContainerStyle={{gap:10}} ListEmptyComponent={<Text style={s.empty}>{error?(error as Error).message:"No saved jobs."}</Text>} renderItem={({item})=><View style={{gap:6}}><Card job={item} onPress={()=>r.push(`/business/jobs/${item.id}`as any)}/><Button label="Remove saved job" onPress={()=>m.mutate({type:"favorite",id:item.id,value:false})}/></View>}/></View>}